{
  "number": 1,
  "title": "Two Sum",
  "difficulty": "easy",
  "description": {
    "en": "Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target. You may assume that each input would have exactly one solution, and you may not use the same element twice. You can return the answer in any order.",
    "zh-CN": "給定一個整數數組 nums 和一個整數目標值 target，請你在該數組中找出和為目標值 target 的那兩個整數，並返回它們的數組下標。你可以假設每種輸入只會對應一個答案。但是，數組中同一個元素在答案裡不能重複出現。你可以按任意順序返回答案。",
    "zh-TW": "給定一個整數陣列 nums 和一個整數目標值 target，請你在該陣列中找出和為目標值 target 的那兩個整數，並返回它們的陣列下標。你可以假設每種輸入只會對應一個答案。但是，陣列中同一個元素在答案裡不能重複出現。你可以按任意順序返回答案。",
    "ja": "整数配列 nums と整数 target が与えられたとき、合計が target になる 2 つの数値のインデックスを返します。各入力には正確に 1 つの解があると仮定でき、同じ要素を 2 回使用することはできません。任意の順序で答えを返すことができます。",
    "es": "Dado un array de enteros nums y un entero target, devuelve los índices de los dos números que suman target. Puedes asumir que cada entrada tendría exactamente una solución, y no puedes usar el mismo elemento dos veces. Puedes devolver la respuesta en cualquier orden.",
    "de": "Gegeben ein Array von Ganzzahlen nums und eine Ganzzahl target, gib die Indizes der zwei Zahlen zurück, die sich zu target addieren. Du kannst annehmen, dass jede Eingabe genau eine Lösung hat, und du darfst dasselbe Element nicht zweimal verwenden. Du kannst die Antwort in beliebiger Reihenfolge zurückgeben.",
    "fr": "Étant donné un tableau d'entiers nums et un entier target, renvoyez les indices des deux nombres qui totalisent target. Vous pouvez supposer que chaque entrée aurait exactement une solution, et vous ne pouvez pas utiliser le même élément deux fois. Vous pouvez renvoyer la réponse dans n'importe quel ordre."
  },
  "aplSolution": "TwoSum ← {(⊃⍸⍺=+/∘.,⍨⍵)}",
  "explanation": {
    "en": "Algorithm: Create all possible pair sums using outer product. Step 1: ⍵∘.+⍵ creates a matrix where element [i,j] is nums[i]+nums[j]. Step 2: Compare this matrix with target (⍺=) to get boolean matrix. Step 3: ⍸ finds the [row,col] indices where value is 1 (true). Step 4: ⊃ extracts the first matching pair of indices. This is O(n²) time and space.",
    "zh-CN": "算法：使用外积创建所有可能的配对和。步骤 1：⍵∘.+⍵ 创建一个矩阵，其中元素 [i,j] 是 nums[i]+nums[j]。步骤 2：将此矩阵与目标值比较 (⍺=) 得到布尔矩阵。步骤 3：⍸ 找到值为 1（真）的 [行,列] 索引。步骤 4：⊃ 提取第一对匹配的索引。时间和空间复杂度都是 O(n²)。",
    "zh-TW": "演算法：使用外積創建所有可能的配對和。步驟 1：⍵∘.+⍵ 創建一個矩陣，其中元素 [i,j] 是 nums[i]+nums[j]。步驟 2：將此矩陣與目標值比較 (⍺=) 得到布林矩陣。步驟 3：⍸ 找到值為 1（真）的 [行,列] 索引。步驟 4：⊃ 提取第一對匹配的索引。時間和空間複雜度都是 O(n²)。",
    "ja": "Algorithm: Create all possible pair sums using outer product. Step 1: ⍵∘.+⍵ creates a matrix where element [i,j] is nums[i]+nums[j]. Step 2: Compare this matrix with target (⍺=) to get boolean matrix. Step 3: ⍸ finds the [row,col] indices where value is 1 (true). Step 4: ⊃ extracts the first matching pair of indices. This is O(n²) time and space.",
    "es": "Algorithm: Create all possible pair sums using outer product. Step 1: ⍵∘.+⍵ creates a matrix where element [i,j] is nums[i]+nums[j]. Step 2: Compare this matrix with target (⍺=) to get boolean matrix. Step 3: ⍸ finds the [row,col] indices where value is 1 (true). Step 4: ⊃ extracts the first matching pair of indices. This is O(n²) time and space.",
    "de": "Algorithm: Create all possible pair sums using outer product. Step 1: ⍵∘.+⍵ creates a matrix where element [i,j] is nums[i]+nums[j]. Step 2: Compare this matrix with target (⍺=) to get boolean matrix. Step 3: ⍸ finds the [row,col] indices where value is 1 (true). Step 4: ⊃ extracts the first matching pair of indices. This is O(n²) time and space.",
    "fr": "Algorithm: Create all possible pair sums using outer product. Step 1: ⍵∘.+⍵ creates a matrix where element [i,j] is nums[i]+nums[j]. Step 2: Compare this matrix with target (⍺=) to get boolean matrix. Step 3: ⍸ finds the [row,col] indices where value is 1 (true). Step 4: ⊃ extracts the first matching pair of indices. This is O(n²) time and space."
  },
  "timeComplexity": "O(n²)",
  "spaceComplexity": "O(n²)",
  "verified": false,
  "testCases": [
    {
      "input": "target=9, nums=2 7 11 15",
      "output": "0 1"
    },
    {
      "input": "target=6, nums=3 2 4",
      "output": "1 2"
    },
    {
      "input": "target=6, nums=3 3",
      "output": "0 1"
    }
  ]
}