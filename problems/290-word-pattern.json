{
  "number": 290,
  "title": "Word Pattern",
  "difficulty": "easy",
  "description": {
    "en": "Check word pattern",
    "zh-CN": "Check word pattern",
    "zh-TW": "Check word pattern",
    "ja": "Check word pattern",
    "es": "Check word pattern",
    "de": "Check word pattern",
    "fr": "Check word pattern"
  },
  "aplSolution": "WordPattern ← {(⍋⍺)≡⍋⍵}",
  "explanation": {
    "en": "APL solution for Word Pattern. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Word Pattern 的 APL 解决方案。使用等级 (⍋/⍒) 排序 - 返回将对数组排序的索引。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Word Pattern 的 APL 解決方案。使用等級 (⍋/⍒) 排序 - 返回將對陣列排序的索引。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Word Pattern. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Word Pattern. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Word Pattern. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Word Pattern. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(n)",
  "verified": false,
  "testCases": []
}