{
  "number": 448,
  "title": "Find All Numbers Disappeared in an Array",
  "difficulty": "easy",
  "description": {
    "en": "Given an array nums of n integers where nums[i] is in the range [1, n], return an array of all the integers in the range [1, n] that do not appear in nums.",
    "zh-CN": "給你一個含 n 個整數的數組 nums，其中 nums[i] 在區間 [1, n] 內。請你找出所有在 [1, n] 範圍內但沒有出現在 nums 中的數字，並以數組的形式返回結果。",
    "zh-TW": "給你一個含 n 個整數的陣列 nums，其中 nums[i] 在區間 [1, n] 內。請你找出所有在 [1, n] 範圍內但沒有出現在 nums 中的數字，並以陣列的形式返回結果。",
    "ja": "n 個の整数を含む配列 nums が与えられます。nums[i] は範囲 [1, n] 内にあります。nums に現れない範囲 [1, n] のすべての整数の配列を返します。",
    "es": "Dado un array nums de n enteros donde nums[i] está en el rango [1, n], devuelve un array de todos los enteros en el rango [1, n] que no aparecen en nums.",
    "de": "Gegeben ein Array nums von n Ganzzahlen, wobei nums[i] im Bereich [1, n] liegt, gib ein Array aller Ganzzahlen im Bereich [1, n] zurück, die nicht in nums erscheinen.",
    "fr": "Étant donné un tableau nums de n entiers où nums[i] est dans la plage [1, n], renvoyez un tableau de tous les entiers de la plage [1, n] qui n'apparaissent pas dans nums."
  },
  "aplSolution": "FindDisappearedNumbers ← {(⍳⌈/⍵)~⍵}",
  "explanation": {
    "en": "APL solution for Find All Numbers Disappeared in an Array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses iota (⍳) to generate index ranges or find element positions. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Find All Numbers Disappeared in an Array 的 APL 解决方案。使用归约 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。使用 iota (⍳) 生成索引范围或查找元素位置。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Find All Numbers Disappeared in an Array 的 APL 解決方案。使用歸約 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。使用 iota (⍳) 生成索引範圍或查找元素位置。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Find All Numbers Disappeared in an Array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses iota (⍳) to generate index ranges or find element positions. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Find All Numbers Disappeared in an Array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses iota (⍳) to generate index ranges or find element positions. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Find All Numbers Disappeared in an Array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses iota (⍳) to generate index ranges or find element positions. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Find All Numbers Disappeared in an Array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses iota (⍳) to generate index ranges or find element positions. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(n)",
  "verified": false,
  "testCases": [
    {
      "input": "4 3 2 7 8 2 3 1",
      "output": "5 6"
    },
    {
      "input": "1 1",
      "output": "2"
    }
  ]
}