{
  "number": 88,
  "title": "Merge Sorted Array",
  "difficulty": "easy",
  "description": {
    "en": "Merge two sorted arrays",
    "zh-CN": "Merge two sorted arrays",
    "zh-TW": "Merge two sorted arrays",
    "ja": "Merge two sorted arrays",
    "es": "Merge two sorted arrays",
    "de": "Merge two sorted arrays",
    "fr": "Merge two sorted arrays"
  },
  "aplSolution": "Merge ← {(⍺,⍵)[⍋⍺,⍵]}",
  "explanation": {
    "en": "APL solution for Merge Sorted Array. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Merge Sorted Array 的 APL 解决方案。使用等级 (⍋/⍒) 排序 - 返回将对数组排序的索引。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Merge Sorted Array 的 APL 解決方案。使用等級 (⍋/⍒) 排序 - 返回將對陣列排序的索引。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Merge Sorted Array. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Merge Sorted Array. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Merge Sorted Array. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Merge Sorted Array. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n+m)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": []
}