{
  "number": 34,
  "title": "First and Last Position",
  "difficulty": "medium",
  "description": {
    "en": "Find first and last position",
    "zh-CN": "Find first and last position",
    "zh-TW": "Find first and last position",
    "ja": "Find first and last position",
    "es": "Find first and last position",
    "de": "Find first and last position",
    "fr": "Find first and last position"
  },
  "aplSolution": "SearchRange ← {(⊃⍸⍺=⍵),(⊃⌽⍸⍺=⍵)}",
  "explanation": {
    "en": "APL solution for First and Last Position. Uses reverse (⌽) to flip array elements. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "First and Last Position 的 APL 解决方案。使用反转 (⌽) 翻转数组元素。使用 where (⍸) 查找真值/非零元素的索引。使用封闭 (⊂) 包装元素或展开 (⊃) 解包/提取。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "First and Last Position 的 APL 解決方案。使用反轉 (⌽) 翻轉陣列元素。使用 where (⍸) 查找真值/非零元素的索引。使用封閉 (⊂) 包裝元素或展開 (⊃) 解包/提取。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for First and Last Position. Uses reverse (⌽) to flip array elements. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for First and Last Position. Uses reverse (⌽) to flip array elements. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for First and Last Position. Uses reverse (⌽) to flip array elements. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for First and Last Position. Uses reverse (⌽) to flip array elements. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(log n)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": []
}