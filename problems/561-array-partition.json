{
  "number": 561,
  "title": "Array Partition",
  "difficulty": "easy",
  "description": {
    "en": "Max sum of min pairs",
    "zh-CN": "Max sum of min pairs",
    "zh-TW": "Max sum of min pairs",
    "ja": "Max sum of min pairs",
    "es": "Max sum of min pairs",
    "de": "Max sum of min pairs",
    "fr": "Max sum of min pairs"
  },
  "aplSolution": "ArrayPairSum ← {+/2↓⍵[⍋⍵]}",
  "explanation": {
    "en": "APL solution for Array Partition. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Array Partition 的 APL 解决方案。使用等级 (⍋/⍒) 排序 - 返回将对数组排序的索引。使用归约 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Array Partition 的 APL 解決方案。使用等級 (⍋/⍒) 排序 - 返回將對陣列排序的索引。使用歸約 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Array Partition. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Array Partition. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Array Partition. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Array Partition. Uses grade (⍋/⍒) for sorting - returns indices that would sort the array. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n log n)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": []
}