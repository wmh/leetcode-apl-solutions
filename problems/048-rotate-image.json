{
  "number": 48,
  "title": "Rotate Image",
  "difficulty": "medium",
  "description": {
    "en": "Rotate NxN matrix 90 degrees clockwise",
    "zh-CN": "Rotate NxN matrix 90 degrees clockwise",
    "zh-TW": "Rotate NxN matrix 90 degrees clockwise",
    "ja": "Rotate NxN matrix 90 degrees clockwise",
    "es": "Rotate NxN matrix 90 degrees clockwise",
    "de": "Rotate NxN matrix 90 degrees clockwise",
    "fr": "Rotate NxN matrix 90 degrees clockwise"
  },
  "aplSolution": "Rotate ← {⌽⍉⍵}",
  "explanation": {
    "en": "APL solution for Rotate Image. Uses reverse (⌽) to flip array elements. Uses transpose (⍉) to swap matrix rows and columns. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Rotate Image 的 APL 解决方案。使用反转 (⌽) 翻转数组元素。使用转置 (⍉) 交换矩阵行和列。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Rotate Image 的 APL 解決方案。使用反轉 (⌽) 翻轉陣列元素。使用轉置 (⍉) 交換矩陣行和列。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Rotate Image. Uses reverse (⌽) to flip array elements. Uses transpose (⍉) to swap matrix rows and columns. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Rotate Image. Uses reverse (⌽) to flip array elements. Uses transpose (⍉) to swap matrix rows and columns. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Rotate Image. Uses reverse (⌽) to flip array elements. Uses transpose (⍉) to swap matrix rows and columns. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Rotate Image. Uses reverse (⌽) to flip array elements. Uses transpose (⍉) to swap matrix rows and columns. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n²)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": []
}