{
  "number": 45,
  "title": "Jump Game II",
  "difficulty": "medium",
  "description": {
    "en": "Min jumps to end",
    "zh-CN": "Min jumps to end",
    "zh-TW": "Min jumps to end",
    "ja": "Min jumps to end",
    "es": "Min jumps to end",
    "de": "Min jumps to end",
    "fr": "Min jumps to end"
  },
  "aplSolution": "Jump ← {+/2≠/0,⍸0<+\\⌈\\⍵}",
  "explanation": {
    "en": "APL solution for Jump Game II. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses scan (\\) to compute running operations: +\\ is running sum, ×\\ is running product, ⌈\\ is running max, ⌊\\ is running min. Uses where (⍸) to find indices of true/non-zero elements. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Jump Game II 的 APL 解决方案。使用归约 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。使用扫描 (\\) 计算运行操作：+\\ 是运行和，×\\ 是运行积，⌈\\ 是运行最大值，⌊\\ 是运行最小值。使用 where (⍸) 查找真值/非零元素的索引。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Jump Game II 的 APL 解決方案。使用歸約 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。使用掃描 (\\) 計算運行操作：+\\ 是運行和，×\\ 是運行積，⌈\\ 是運行最大值，⌊\\ 是運行最小值。使用 where (⍸) 查找真值/非零元素的索引。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Jump Game II. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses scan (\\) to compute running operations: +\\ is running sum, ×\\ is running product, ⌈\\ is running max, ⌊\\ is running min. Uses where (⍸) to find indices of true/non-zero elements. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Jump Game II. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses scan (\\) to compute running operations: +\\ is running sum, ×\\ is running product, ⌈\\ is running max, ⌊\\ is running min. Uses where (⍸) to find indices of true/non-zero elements. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Jump Game II. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses scan (\\) to compute running operations: +\\ is running sum, ×\\ is running product, ⌈\\ is running max, ⌊\\ is running min. Uses where (⍸) to find indices of true/non-zero elements. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Jump Game II. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Uses scan (\\) to compute running operations: +\\ is running sum, ×\\ is running product, ⌈\\ is running max, ⌊\\ is running min. Uses where (⍸) to find indices of true/non-zero elements. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": []
}