{
  "number": 739,
  "title": "Daily Temperatures",
  "difficulty": "medium",
  "description": {
    "en": "Find how many days until warmer temperature.",
    "zh-CN": "[题目739] Find how many days until warmer temperature....",
    "zh-TW": "[題目739] Find how many days until warmer temperature....",
    "ja": "[問題739] Find how many days until warmer temperature....",
    "es": "[Problema 739] Find how many days until warmer temperature....",
    "de": "[Problem 739] Find how many days until warmer temperature....",
    "fr": "[Problème 739] Find how many days until warmer temperature...."
  },
  "aplSolution": "DailyTemperatures ← {\n    n←≢⍵\n    result←n⍴0\n    {result[⍵]←⊃⍸⍵<⍵↓⍵}¨⍳n\n    result\n}",
  "explanation": {
    "en": "APL solution for Daily Temperatures. Uses where (⍸) to find indices of true/non-zero elements. Uses tally (≢) to count array length. Uses iota (⍳) to generate index ranges or find element positions. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Daily Temperatures 的 APL 解决方案。使用 where (⍸) 查找真值/非零元素的索引。使用 tally (≢) 计算数组长度。使用 iota (⍳) 生成索引范围或查找元素位置。使用封闭 (⊂) 包装元素或展开 (⊃) 解包/提取。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Daily Temperatures 的 APL 解決方案。使用 where (⍸) 查找真值/非零元素的索引。使用 tally (≢) 計算陣列長度。使用 iota (⍳) 生成索引範圍或查找元素位置。使用封閉 (⊂) 包裝元素或展開 (⊃) 解包/提取。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Daily Temperatures. Uses where (⍸) to find indices of true/non-zero elements. Uses tally (≢) to count array length. Uses iota (⍳) to generate index ranges or find element positions. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Daily Temperatures. Uses where (⍸) to find indices of true/non-zero elements. Uses tally (≢) to count array length. Uses iota (⍳) to generate index ranges or find element positions. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Daily Temperatures. Uses where (⍸) to find indices of true/non-zero elements. Uses tally (≢) to count array length. Uses iota (⍳) to generate index ranges or find element positions. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Daily Temperatures. Uses where (⍸) to find indices of true/non-zero elements. Uses tally (≢) to count array length. Uses iota (⍳) to generate index ranges or find element positions. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n²)",
  "spaceComplexity": "O(n)"
}