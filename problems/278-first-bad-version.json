{
  "number": 278,
  "title": "First Bad Version",
  "difficulty": "easy",
  "description": {
    "en": "Find first bad version",
    "zh-CN": "Find first bad version",
    "zh-TW": "Find first bad version",
    "ja": "Find first bad version",
    "es": "Find first bad version",
    "de": "Find first bad version",
    "fr": "Find first bad version"
  },
  "aplSolution": "FirstBadVersion ← {⊃⍸⍵}",
  "explanation": {
    "en": "APL solution for First Bad Version. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "First Bad Version 的 APL 解决方案。使用 where (⍸) 查找真值/非零元素的索引。使用封闭 (⊂) 包装元素或展开 (⊃) 解包/提取。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "First Bad Version 的 APL 解決方案。使用 where (⍸) 查找真值/非零元素的索引。使用封閉 (⊂) 包裝元素或展開 (⊃) 解包/提取。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for First Bad Version. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for First Bad Version. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for First Bad Version. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for First Bad Version. Uses where (⍸) to find indices of true/non-zero elements. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(log n)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": []
}