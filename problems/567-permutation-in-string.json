{
  "number": 567,
  "title": "Permutation in String",
  "difficulty": "medium",
  "description": {
    "en": "Check if s2 contains a permutation of s1.",
    "zh-CN": "[题目567] Check if s2 contains a permutation of s1....",
    "zh-TW": "[題目567] Check if s2 contains a permutation of s1....",
    "ja": "[問題567] Check if s2 contains a permutation of s1....",
    "es": "[Problema 567] Check if s2 contains a permutation of s1....",
    "de": "[Problem 567] Check if s2 contains a permutation of s1....",
    "fr": "[Problème 567] Check if s2 contains a permutation of s1...."
  },
  "aplSolution": "CheckInclusion ← {\n    (≢⍺)≤≢⍵:∨/{(∧/⍺∊⍵)∧∧/⍵∊⍺}¨(≢⍺)↑¨(≢⍵)↓¨⊂⍵\n    0\n}",
  "explanation": {
    "en": "APL solution for Permutation in String. Uses tally (≢) to count array length. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Permutation in String 的 APL 解决方案。使用 tally (≢) 计算数组长度。使用封闭 (⊂) 包装元素或展开 (⊃) 解包/提取。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Permutation in String 的 APL 解決方案。使用 tally (≢) 計算陣列長度。使用封閉 (⊂) 包裝元素或展開 (⊃) 解包/提取。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Permutation in String. Uses tally (≢) to count array length. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Permutation in String. Uses tally (≢) to count array length. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Permutation in String. Uses tally (≢) to count array length. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Permutation in String. Uses tally (≢) to count array length. Uses enclose (⊂) to wrap elements or disclose (⊃) to unwrap/extract. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n*m)",
  "spaceComplexity": "O(1)"
}