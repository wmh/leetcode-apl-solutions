{
  "number": 191,
  "title": "Number of 1 Bits",
  "difficulty": "easy",
  "description": {
    "en": "Write a function that takes the binary representation of a positive integer and returns the number of set bits it has (also known as the Hamming weight).",
    "zh-CN": "編寫一個函數，輸入是一個無符號整數（以二進制串的形式），返回其二進制表達式中設置位的個數（也被稱為漢明重量）。",
    "zh-TW": "編寫一個函數，輸入是一個無符號整數（以二進位串的形式），返回其二進位表達式中設置位的個數（也被稱為漢明重量）。",
    "ja": "正の整数の 2 進数表現を受け取り、セットされているビット数（ハミング重みとも呼ばれます）を返す関数を作成します。",
    "es": "Escribe una función que tome la representación binaria de un entero positivo y devuelva el número de bits establecidos que tiene (también conocido como peso de Hamming).",
    "de": "Schreibe eine Funktion, die die binäre Darstellung einer positiven Ganzzahl nimmt und die Anzahl der gesetzten Bits zurückgibt (auch als Hamming-Gewicht bekannt).",
    "fr": "Écrivez une fonction qui prend la représentation binaire d'un entier positif et renvoie le nombre de bits définis qu'il a (également connu sous le nom de poids de Hamming)."
  },
  "aplSolution": "HammingWeight ← {+/2⊥⍣¯1⊢⍵}",
  "explanation": {
    "en": "APL solution for Number of 1 Bits. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Number of 1 Bits 的 APL 解决方案。使用归约 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Number of 1 Bits 的 APL 解決方案。使用歸約 (/) 聚合值：+/ 求和，×/ 相乘，⌈/ 找最大值，⌊/ 找最小值。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Number of 1 Bits. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Number of 1 Bits. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Number of 1 Bits. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Number of 1 Bits. Uses reduction (/) to aggregate values: +/ sums, ×/ multiplies, ⌈/ finds max, ⌊/ finds min. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(1)",
  "spaceComplexity": "O(1)",
  "verified": false,
  "testCases": [
    {
      "input": "11",
      "output": "3"
    },
    {
      "input": "128",
      "output": "1"
    },
    {
      "input": "2147483645",
      "output": "30"
    }
  ]
}