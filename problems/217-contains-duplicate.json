{
  "number": 217,
  "title": "Contains Duplicate",
  "difficulty": "easy",
  "description": {
    "en": "Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.",
    "zh-CN": "給定一個整數數組 nums，如果任何值在數組中出現至少兩次，則返回 true；如果每個元素都不同，則返回 false。",
    "zh-TW": "給定一個整數陣列 nums，如果任何值在陣列中出現至少兩次，則返回 true；如果每個元素都不同，則返回 false。",
    "ja": "整数配列 nums が与えられたとき、配列内に少なくとも 2 回出現する値があれば true を返し、すべての要素が異なる場合は false を返します。",
    "es": "Dado un array de enteros nums, devuelve true si algún valor aparece al menos dos veces en el array, y devuelve false si cada elemento es distinto.",
    "de": "Gegeben ein Integer-Array nums, gib true zurück, wenn ein Wert mindestens zweimal im Array erscheint, und gib false zurück, wenn jedes Element eindeutig ist.",
    "fr": "Étant donné un tableau d'entiers nums, renvoyez true si une valeur apparaît au moins deux fois dans le tableau, et renvoyez false si chaque élément est distinct."
  },
  "aplSolution": "ContainsDuplicate ← {(≢⍵)≠≢∪⍵}",
  "explanation": {
    "en": "Check for duplicates by comparing array length with unique length. Step 1: ≢⍵ counts total elements in original array. Step 2: ∪⍵ removes duplicate elements, keeping only unique values. Step 3: ≢∪⍵ counts unique elements. Step 4: ≠ compares the two counts - if they're different, duplicates exist. Returns 1 (true) if duplicates found, 0 (false) otherwise. Example: [1,2,3,1] has length 4 but 3 unique values, so 4≠3 returns 1.",
    "zh-CN": "通过比较数组长度与唯一长度来检查重复项。步骤 1：≢⍵ 计算原始数组中的总元素数。步骤 2：∪⍵ 删除重复元素，仅保留唯一值。步骤 3：≢∪⍵ 计算唯一元素数。步骤 4：≠ 比较两个计数 - 如果它们不同，则存在重复项。如果找到重复项则返回 1（真），否则返回 0（假）。示例：[1,2,3,1] 长度为 4 但只有 3 个唯一值，所以 4≠3 返回 1。",
    "zh-TW": "通過比較陣列長度與唯一長度來檢查重複項。步驟 1：≢⍵ 計算原始陣列中的總元素數。步驟 2：∪⍵ 刪除重複元素，僅保留唯一值。步驟 3：≢∪⍵ 計算唯一元素數。步驟 4：≠ 比較兩個計數 - 如果它們不同，則存在重複項。如果找到重複項則返回 1（真），否則返回 0（假）。示例：[1,2,3,1] 長度為 4 但只有 3 個唯一值，所以 4≠3 返回 1。",
    "ja": "Check for duplicates by comparing array length with unique length. Step 1: ≢⍵ counts total elements in original array. Step 2: ∪⍵ removes duplicate elements, keeping only unique values. Step 3: ≢∪⍵ counts unique elements. Step 4: ≠ compares the two counts - if they're different, duplicates exist. Returns 1 (true) if duplicates found, 0 (false) otherwise. Example: [1,2,3,1] has length 4 but 3 unique values, so 4≠3 returns 1.",
    "es": "Check for duplicates by comparing array length with unique length. Step 1: ≢⍵ counts total elements in original array. Step 2: ∪⍵ removes duplicate elements, keeping only unique values. Step 3: ≢∪⍵ counts unique elements. Step 4: ≠ compares the two counts - if they're different, duplicates exist. Returns 1 (true) if duplicates found, 0 (false) otherwise. Example: [1,2,3,1] has length 4 but 3 unique values, so 4≠3 returns 1.",
    "de": "Check for duplicates by comparing array length with unique length. Step 1: ≢⍵ counts total elements in original array. Step 2: ∪⍵ removes duplicate elements, keeping only unique values. Step 3: ≢∪⍵ counts unique elements. Step 4: ≠ compares the two counts - if they're different, duplicates exist. Returns 1 (true) if duplicates found, 0 (false) otherwise. Example: [1,2,3,1] has length 4 but 3 unique values, so 4≠3 returns 1.",
    "fr": "Check for duplicates by comparing array length with unique length. Step 1: ≢⍵ counts total elements in original array. Step 2: ∪⍵ removes duplicate elements, keeping only unique values. Step 3: ≢∪⍵ counts unique elements. Step 4: ≠ compares the two counts - if they're different, duplicates exist. Returns 1 (true) if duplicates found, 0 (false) otherwise. Example: [1,2,3,1] has length 4 but 3 unique values, so 4≠3 returns 1."
  },
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(n)",
  "verified": false,
  "testCases": [
    {
      "input": "1 2 3 1",
      "output": "1"
    },
    {
      "input": "1 2 3 4",
      "output": "0"
    },
    {
      "input": "1 1 1 3 3 4 3 2 4 2",
      "output": "1"
    }
  ]
}