{
  "number": 217,
  "title": "Contains Duplicate",
  "difficulty": "easy",
  "description": {
    "en": "Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.",
    "zh-CN": "給定一個整數數組 nums，如果任何值在數組中出現至少兩次，則返回 true；如果每個元素都不同，則返回 false。",
    "zh-TW": "給定一個整數陣列 nums，如果任何值在陣列中出現至少兩次，則返回 true；如果每個元素都不同，則返回 false。",
    "ja": "整数配列 nums が与えられたとき、配列内に少なくとも 2 回出現する値があれば true を返し、すべての要素が異なる場合は false を返します。",
    "es": "Dado un array de enteros nums, devuelve true si algún valor aparece al menos dos veces en el array, y devuelve false si cada elemento es distinto.",
    "de": "Gegeben ein Integer-Array nums, gib true zurück, wenn ein Wert mindestens zweimal im Array erscheint, und gib false zurück, wenn jedes Element eindeutig ist.",
    "fr": "Étant donné un tableau d'entiers nums, renvoyez true si une valeur apparaît au moins deux fois dans le tableau, et renvoyez false si chaque élément est distinct."
  },
  "aplSolution": "ContainsDuplicate ← {(≢⍵)≠≢∪⍵}",
  "explanation": {
    "en": "APL solution for Contains Duplicate. Uses unique (∪) to remove duplicate elements. Uses tally (≢) to count array length. Implementation uses APL's array-oriented primitives for concise expression.",
    "zh-CN": "Contains Duplicate 的 APL 解决方案。使用 unique (∪) 去除重复元素。使用 tally (≢) 计算数组长度。实现使用 APL 的面向数组原语进行简洁表达。",
    "zh-TW": "Contains Duplicate 的 APL 解決方案。使用 unique (∪) 去除重複元素。使用 tally (≢) 計算陣列長度。實現使用 APL 的面向陣列原語進行簡潔表達。",
    "ja": "APL solution for Contains Duplicate. Uses unique (∪) to remove duplicate elements. Uses tally (≢) to count array length. Implementation uses APL's array-oriented primitives for concise expression.",
    "es": "APL solution for Contains Duplicate. Uses unique (∪) to remove duplicate elements. Uses tally (≢) to count array length. Implementation uses APL's array-oriented primitives for concise expression.",
    "de": "APL solution for Contains Duplicate. Uses unique (∪) to remove duplicate elements. Uses tally (≢) to count array length. Implementation uses APL's array-oriented primitives for concise expression.",
    "fr": "APL solution for Contains Duplicate. Uses unique (∪) to remove duplicate elements. Uses tally (≢) to count array length. Implementation uses APL's array-oriented primitives for concise expression."
  },
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(n)",
  "verified": false,
  "testCases": [
    {
      "input": "1 2 3 1",
      "output": "1"
    },
    {
      "input": "1 2 3 4",
      "output": "0"
    },
    {
      "input": "1 1 1 3 3 4 3 2 4 2",
      "output": "1"
    }
  ]
}